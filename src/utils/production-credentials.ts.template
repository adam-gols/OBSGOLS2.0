/**
 * Production Google Sheets Configuration - TEMPLATE
 * 
 * INSTRUCTIONS:
 * 1. Copy this file to 'production-credentials.ts'
 * 2. Replace the placeholder values with your actual credentials
 * 3. Generate Base64 encoding of your service account JSON using:
 *    echo 'YOUR_JSON_CONTENT' | base64
 * 
 * NEVER commit the actual production-credentials.ts file!
 */

export const PRODUCTION_SHEETS_CONFIG = {
  // Your Google Cloud Project ID
  projectId: 'your-project-id',
  
  // Base64-encoded service account JSON credentials
  // Generate this by: echo '{"type":"service_account",...}' | base64
  serviceAccountBase64: 'YOUR_BASE64_ENCODED_SERVICE_ACCOUNT_JSON_HERE',
  
  // File Cabinet spreadsheet ID (from the URL)
  fileCabinetSpreadsheetId: '1yIMVWLxVRwsTnUlRNuIKIhesgrL9rvTZheq7-U-zNM0',
  
  // Default range for events in File Cabinet
  eventsRange: 'Events!A:B',
  
  // Scopes required for Google Sheets API
  scopes: [
    'https://www.googleapis.com/auth/spreadsheets.readonly'
  ]
};

export const PRODUCTION_SERVICE_ACCOUNT_CREDENTIALS = JSON.parse(
  typeof window !== 'undefined' && window.atob 
    ? window.atob(PRODUCTION_SHEETS_CONFIG.serviceAccountBase64)
    : Buffer.from(PRODUCTION_SHEETS_CONFIG.serviceAccountBase64, 'base64').toString('utf-8')
);
